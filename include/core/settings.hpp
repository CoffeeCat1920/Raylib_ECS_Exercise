#pragma once

inline constexpr int BLOCK = 32;

inline constexpr int SCREEN_WIDTH = 10;
inline constexpr int SCREEN_HEIGHT = 6;

inline constexpr int RENDERING_WIDTH = SCREEN_WIDTH * BLOCK * 4 ;
inline constexpr int RENDERING_HEIGHT = SCREEN_HEIGHT * BLOCK * 4;

inline constexpr int FONT_SIZE = 32;
